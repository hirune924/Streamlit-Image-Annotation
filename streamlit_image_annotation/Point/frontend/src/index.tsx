import React from "react"
import ReactDOM from "react-dom"
import PointDet from "./PointDet"

ReactDOM.render(
  <React.StrictMode>
    <PointDet />
  </React.StrictMode>,
  document.getElementById("root")
)
